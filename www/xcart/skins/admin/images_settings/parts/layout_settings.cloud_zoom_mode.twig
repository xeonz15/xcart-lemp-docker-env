{##
 # Cloud zoom switcher
 #
 # @ListChild (list="images_settings", weight="50")
 #}

{% if this.isCloudZoomAllowed() %}
  <li>{{ widget('XLite\\View\\FormField\\Select\\CloudZoomMode', label='Cloud Zoom mode', fieldName='cloud_zoom_mode', value=this.getCloudZoomMode()) }}</li>
{% endif %}
